# ==============================================================================
# .env.docker - Configuração para Deploy Docker
# ==============================================================================

# Ambiente
NODE_ENV=production

# URL base da API (OBRIGATÓRIO - descomente e configure)
NEXT_PUBLIC_API_BASE_URL=https://abitus-api.pjc.mt.gov.br/v1

# Timeout da API em milissegundos
NEXT_PUBLIC_API_TIMEOUT=30000

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1

# Port Configuration (usado pelo Docker)
PORT=3000
HOSTNAME=0.0.0.0

# Observações:
# - Variáveis que começam com NEXT_PUBLIC_ são expostas ao cliente.
# - Em produção, prefira injetar secrets via provider (Vercel/Secrets Manager) em vez de arquivos .env.
